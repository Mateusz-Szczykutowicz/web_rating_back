import { UserConstrollerI } from "../interfaces/user.interface";

const UserController: UserConstrollerI = {
    getAllUsers: (req, res) => {},
    getOneUser: (req, res) => {},
};

export default UserController;
